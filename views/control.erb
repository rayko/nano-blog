<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>NanoBlog Control</title>
    <script src="/assets/control.js"></script>
    <link href="/assets/control.css" rel="stylesheet">
  </head>

  <body>
    <div class="main-container">
      <h1>nanoBlog Control</h1>
      
      <div class="form-container">
        <div class="form-title">Generic Post</div>
        <form class="common-form" action="/control/generic-push" method="post">
          <select name="severity">
            <% LogEntry::SEVERITIES.each do |item| %>
              <option value="<%= item %>"><%= item %></option>
            <% end %>
          </select>

          <select name="component_id">
            <option value="" selected></option>
            <% Component.all.each do |item| %>
              <option value="<%= item.id %>"><%= item.name %></option>
            <% end %>
          </select>

          <input type="text" id="message" name="message" placeholder="Message here" size=80>
          <input type="submit" value="Push">
        </form>
      </div>

      <div class="form-container">
        <div class="form-title">Add Component Name</div>
        <form class="common-form" action="/control/components" method="post">
          <input type="text" id="name" name="name" placeholder="Enter name">
          <input type="submit" value="Add">
        </form>
      </div>

      <div class="form-container">
        <div class="form-title">Components</div>
        <ul class="record-list">
          <% Component.all.each do |item| %>
            <% next if item.name == 'MONITOR' %>
            <li class="record-item">
              <%= item.id %> -
              <%= item.name %> -
              <form action="/control/components/<%= item.id %>" method="post">
                <input type="hidden" name="_method" value="DELETE">
                <input class="delete-buttom" type="submit" value="Remove">
              </form>
            </li>
          <% end %>
        </ul>
      </div>

      <div class="generic-post-form-container">

      </div>
    </div>
  </body>

</html>
